import { Label, styled } from "tamagui";

export const InputLabel = styled(Label, {
    color: '$black',
    fontWeight: '$heading',
    fontSize: '$sm',
    lineHeight: '$sm',
    fontFamily: '$button',
});
